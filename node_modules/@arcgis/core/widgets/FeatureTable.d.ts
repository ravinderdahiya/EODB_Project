import type Collection from "../core/Collection.js";
import type Relationship from "../layers/support/Relationship.js";
import type AttributeTableTemplate from "../tables/AttributeTableTemplate.js";
import type TimeExtent from "../time/TimeExtent.js";
import type LinkChartView from "../views/LinkChartView.js";
import type SelectionManager from "../views/SelectionManager.js";
import type LayerView from "../views/layers/LayerView.js";
import type Widget from "./Widget.js";
import type ActionColumn from "./FeatureTable/ActionColumn.js";
import type AttachmentsColumn from "./FeatureTable/AttachmentsColumn.js";
import type FeatureTableViewModel from "./FeatureTable/FeatureTableViewModel.js";
import type FieldColumn from "./FeatureTable/FieldColumn.js";
import type RelationshipColumn from "./FeatureTable/RelationshipColumn.js";
import type VisibleElements from "./FeatureTable/VisibleElements.js";
import type GroupColumn from "./FeatureTable/Grid/GroupColumn.js";
import type AttachmentsViewOptions from "./FeatureTable/support/AttachmentsViewOptions.js";
import type TableTemplate from "./FeatureTable/support/TableTemplate.js";
import type { Icon } from "@esri/calcite-components/components/calcite-icon";
import type { ReadonlyArrayOrCollection } from "../core/Collection.js";
import type { GeometryUnion } from "../geometry/types.js";
import type { TimeZone } from "../time/types.js";
import type { MapViewOrSceneView } from "../views/MapViewOrSceneView.js";
import type { ObjectId } from "../views/types.js";
import type { WidgetProperties } from "./Widget.js";
import type { FeatureTableViewModelEvents, FeatureTableViewModelProperties } from "./FeatureTable/FeatureTableViewModel.js";
import type { Direction, DiscardPendingEditsParameters, FeatureStoreEdits, FeatureTableState, GridMultiSortPriority, GridSupportedColumns, TableMenuConfig } from "./FeatureTable/Grid/types.js";
import type { ActionColumnConfig, ColumnSortOrder, FeatureStoreItem, FeatureTableSupportedColumn, FeatureTableSupportedFilters, FeatureTableSupportedLayer } from "./FeatureTable/support/types.js";
import type { AttributeTableTemplateProperties } from "../tables/AttributeTableTemplate.js";
import type { MeshProperties } from "../geometry/Mesh.js";
import type { PolylineProperties } from "../geometry/Polyline.js";
import type { PolygonProperties } from "../geometry/Polygon.js";
import type { PointProperties } from "../geometry/Point.js";
import type { MultipointProperties } from "../geometry/Multipoint.js";
import type { ExtentProperties } from "../geometry/Extent.js";
import type { TableTemplateProperties } from "./FeatureTable/support/TableTemplate.js";
import type { TimeExtentProperties } from "../time/TimeExtent.js";
import type { VisibleElementsProperties } from "./FeatureTable/VisibleElements.js";

export interface FeatureTableProperties extends WidgetProperties, Partial<Pick<FeatureTable, "actionColumnConfig" | "attachmentsEnabled" | "attachmentsViewOptions" | "autoRefreshEnabled" | "autoSaveEnabled" | "columnPerformanceModeEnabled" | "columnReorderingEnabled" | "definitionExpression" | "description" | "disabled" | "editingEnabled" | "filterBySelectionEnabled" | "highlightEnabled" | "initialSize" | "layer" | "layers" | "maxSize" | "menuConfig" | "multipleSelectionEnabled" | "multipleSortPriority" | "multiSortEnabled" | "navigationScale" | "noDataMessage" | "outFields" | "pageSize" | "paginationEnabled" | "relatedRecordsEnabled" | "returnGeometryEnabled" | "returnMEnabled" | "returnZEnabled" | "selectionManager" | "syncTemplateOnChangesEnabled" | "syncViewSelection" | "syncWithViewSelectionManager" | "timeZone" | "title" | "view">> {
  /**
   * Use this property to configure how columns display within the table in regard to visibility, column order, and sorting.
   *
   * > [!WARNING]
   * >
   * > This property differs from the [tableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableTemplate) property. The `tableTemplate` property provides more fine-grained control over how the table is rendered within the application by offering more advanced configurations such as custom cell rendering, column formatting, and more. It is useful for application-level development that remains within an application. Use the [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate) property to access the table's settings across different applications. By using this property, the settings can be saved within a webmap or layer. Please refer to the [AttributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/tables/AttributeTableTemplate/) and [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/) documentation for more information.
   *
   * @since 4.31
   * @see [Sample - FeatureTable Component](https://developers.arcgis.com/javascript/latest/sample-code/feature-table/)
   */
  attributeTableTemplate?: AttributeTableTemplateProperties | null;
  /**
   * The ID or node representing the DOM element containing the widget.
   * Take note that this property can only be set once. The snippets below provide some examples for setting this.
   *
   * > [!WARNING]
   * >
   * > The [FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/) widget's container property must be set within its constructor and not via the [view.ui.add()](https://developers.arcgis.com/javascript/latest/references/core/views/ui/UI/#add) method.
   *
   * @example
   * // Create the HTML div element programmatically at runtime and set to the widget's container
   * const featureTable = new FeatureTable({
   *   view: view,
   *   container: document.createElement("tableDiv")
   * });
   * @example
   * // Specify an already-defined HTML div element in the widget's container
   *
   * const featureTable = new FeatureTable({
   *   view: view,
   *   container: featureTableDiv
   * });
   *
   * // HTML markup
   * <body>
   *   <div id="viewDiv"></div>
   *   <div class="container">
   *     <div id="tableDiv"></div>
   *   </div>
   * </body>
   * @example
   * // Create the HTML div element programmatically at runtime and set to the widget's container
   * const basemapGallery = new BasemapGallery({
   *   view: view,
   *   container: document.createElement("div")
   * });
   *
   * // Add the widget to the top-right corner of the view
   * view.ui.add(basemapGallery, {
   *   position: "top-right"
   * });
   * @example
   * // Specify an already-defined HTML div element in the widget's container
   *
   * const basemapGallery = new BasemapGallery({
   *   view: view,
   *   container: basemapGalleryDiv
   * });
   *
   * // Add the widget to the top-right corner of the view
   * view.ui.add(basemapGallery, {
   *   position: "top-right"
   * });
   *
   * // HTML markup
   * <body>
   *   <div id="viewDiv"></div>
   *   <div id="basemapGalleryDiv"></div>
   * </body>
   * @example
   * // Specify the widget while adding to the view's UI
   * const basemapGallery = new BasemapGallery({
   *   view: view
   * });
   *
   * // Add the widget to the top-right corner of the view
   * view.ui.add(basemapGallery, {
   *   position: "top-right"
   * });
   */
  container?: HTMLElement | string | null;
  /**
   * Set this property to filter the features displayed in the table. It accepts a [Geometry](https://developers.arcgis.com/javascript/latest/references/core/geometry/Geometry/), e.g. [Extent](https://developers.arcgis.com/javascript/latest/references/core/geometry/Extent/), and uses it as a spatial filter. When modifying this property, the `FeatureTable` will completely refresh and re-query for all features.
   *
   * @since 4.19
   * @see [Sample - FeatureTable using a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @example
   * // Listen for when the view is stationary.
   * // If true, check the view's extent and set
   * // the table to display only the attributes
   * // for the features falling within this extent.
   *
   * reactiveUtils.when( () => view.stationary === true,
   * () => {
   *   // Get the new extent of view/map whenever map is updated.
   *   if (view.extent) {
   *     // Filter out and show only the visible features in the feature table.
   *     featureTable.filterGeometry = view.extent;
   *   }
   * }, { initial: true });
   */
  filterGeometry?: ((ExtentProperties & { type: "extent" }) | (MultipointProperties & { type: "multipoint" }) | (PointProperties & { type: "point" }) | (PolygonProperties & { type: "polygon" }) | (PolylineProperties & { type: "polyline" }) | (MeshProperties & { type: "mesh" })) | null;
  /**
   * A collection of string values which indicate [field.names](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Field/) that should be hidden within the table. By default fields such as `CreationDate`, `Creator`, `EditDate`, `Editor`, and `GlobalID` do not show. If these fields are needed, set them via [TableTemplate.columnTemplates](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/#columnTemplates). In this case, it is required to set column template's [FieldColumnTemplate.visible](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#visible) property to `true`.
   *
   * @since 4.16
   * @see [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/)
   * @see [FieldColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/)
   * @example
   * const featureTable = new FeatureTable({
   *   layer: featureLayer,
   *   view: view,
   *   hiddenFields: ["Primary_Type", "incident_date"], // will not show these two fields within the table
   *   container: document.getElementById("tableDiv")
   * });
   * @example
   * // Set this syntax if needing to display a default hidden field, e.g. 'CreationDate`
   * const featureTable = new FeatureTable({
   *   layer: featureLayer,
   *   view: view,
   *   tableTemplate: { // autocastable to TableTemplate
   *     columnTemplates: [ // takes an array of FieldColumnTemplate and GroupColumnTemplate
   *     { //autocastable to FieldColumnTemplate
   *       type: "field",
   *       fieldName: "date_created",
   *       label: "Date created",
   *       visible: true
   *     }]
   *   }
   *   container: document.getElementById("tableDiv")
   * });
   */
  hiddenFields?: ReadonlyArrayOrCollection<string>;
  /**
   * This property accepts and returns a collection of feature object IDs. Use this to access and control which features are currently selected in the table and subsequently highlighted within the map. Once an application sets a collection of object IDs, the table will select the corresponding row and highlight its feature within the map.
   *
   * @since 4.25
   * @see [highlightEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightEnabled)
   * @see [Sample - FeatureTable using a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/)
   * @see [Sample - FeatureTable with popup interaction](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-popup-interaction/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @see [Sample - FeatureTable with row highlighting](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-row-highlights/)
   * @example
   * // This example instantiates the table with highlighted features
   * const featureTable = new FeatureTable({
   *   view: view,
   *   layer: featureLayer,
   *   container: "tableDiv",
   *   highlightIds
   * });
   *
   * // Push the object IDs into a collection and select
   * featureTable.highlightIds.push(2, 3, 4);
   *
   * // Listen for changes in the collection of highlighted features
   * featureTable.highlightIds.on("change", (event) => {
   *   console.log("features selected", event.added);
   *   console.log("features deselected", event.removed);
   * });
   */
  highlightIds?: ReadonlyArrayOrCollection<ObjectId>;
  /**
   * The string value which indicates the displayed [icon](https://developers.arcgis.com/calcite-design-system/icons/).
   *
   * @default "table"
   * @since 4.27
   * @see [Calcite Icon Search](https://developers.arcgis.com/calcite-design-system/icons/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @see [Sample - FeatureTable with custom content](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-custom-content/)
   * @see [Calcite Icon Search](https://developers.arcgis.com/calcite-design-system/icons/)
   */
  icon?: Icon["icon"] | null;
  /**
   * The widget's default label.
   *
   * @since 4.11
   */
  label?: string | null;
  /**
   * This property accepts and returns a collection of feature object IDs. Use this to access and control which features are currently visible in the table.
   * When the collection is empty, all potential rows are displayed. Modifying object IDs is not supported while [filterBySelectionEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterBySelectionEnabled) is `true` as these properties are mutually exclusive.
   * This filter can also be combined with [filterGeometry](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterGeometry) to display features that satisfy both conditions.
   *
   * @since 4.30
   */
  objectIds?: ReadonlyArrayOrCollection<ObjectId>;
  /**
   * Represents the index of the page of the feature currently being displayed. The number of features per page can be adjusted by modifying the [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize). [Pagination](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled) must be enabled or the value of this property may not be not reliable due to virtualization of visible pages.
   *
   * @since 4.31
   */
  pageIndex?: number | number;
  /**
   * This property accepts and returns a collection of feature object IDs. It is used to access and control which rows display a darker background, i.e., highlighted. Take note that highlighted rows are not considered selected as this property is independent of the table's selection state. Use the [highlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightIds) property to choose rows. Setting `rowHighlightIds` applies an alternative highlight style to an entire row or rows.
   *
   * >
   * > Use this property to highlight rows based on an event or action from another component (e.g. the view). For example, purposefully highlighting rows when hovering over a feature on the map or applying a "filter" via another widget to call out specific rows in the table. Generally, this property is not modified based on table events but rather an event or action from a different component.
   *
   * @since 4.30
   * @example
   * // This snippet adds an event listener which highlights (not selects) a row in the table
   * // when a mouse pointer hovers over its corresponding row.
   * featureTable.on("cell-pointerover", (event) => {
   * // Add the event's feature to the rowHighlightIds collection if it's not already included
   *   if (!featureTable.rowHighlightIds.includes(event.objectId)) {
   *     featureTable.rowHighlightIds.push(event.objectId);
   *   }
   * });
   * // This snippet adds an event listener which removes the highlight from a row in the table
   * // when a mouse pointer moves out of its corresponding row.
   * featureTable.on("cell-pointerout", (event) => {
   *   // Remove the event's feature from the rowHighlightIds collection if it's included
   *   const index = featureTable.rowHighlightIds.indexOf(event.objectId);
   *   if (index > -1) {
   *     featureTable.rowHighlightIds.splice(index, 1);
   *   }
   * });
   */
  rowHighlightIds?: ReadonlyArrayOrCollection<ObjectId>;
  /**
   * A reference to top-level controller table. This property is applicable if this table is a related table, nested within and controlled by another table widget.
   *
   * @since 4.31
   */
  tableController?: FeatureTable | null;
  /**
   * A reference to the instance of a table widget that is directly associated with this table. This only applies if this table is nested within another table widget. The parent table is responsible for creating this child table instance. The child table reacts to state changes on the parent table, including row selection.
   *
   * The parent table that this property references may also be nested, meaning it would refer to a different parent table. The top-level table can always be referenced via the [tableController](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableController) property. If only one nested table exists, then the values of [tableController](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableController) and `tableParent` are the same.
   *
   * @since 4.31
   */
  tableParent?: FeatureTable | null;
  /**
   * The associated [template](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/) used for the feature table.
   *
   * The [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/) is where you configure how the feature table should display and set any associated properties for the table and its columns.
   *
   * > [!WARNING]
   * >
   * > The `TableTemplate` provides more fine-grained control over how the table is rendered within the application by offering more advanced configurations such as custom cell rendering, column formatting, and more. `TableTemplate` is useful for application-level development that remains within an application. This property differs from the [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate) property as that property should be used to access the table's settings across different applications. By using [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate), the settings can be saved within a webmap or layer. Please refer to the [AttributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/tables/AttributeTableTemplate/) and [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/)  documentation for more information.
   *
   * > [!WARNING]
   * >
   * > Take note that it is required to set the `type` property when creating column templates.
   *
   * @since
   * 4.24
   *
   * > [!WARNING]
   * >
   * > Take note that it is required to set the `type` property when creating column templates.
   * @example
   * const tableTemplate = new TableTemplate({
   *   columnTemplates: [ // takes an array of FieldColumnTemplate and GroupColumnTemplate
   *   { // autocasts to FieldColumnTemplate
   *     type: "field", // This must be set when creating field column templates
   *     fieldName: "ObjectId",
   *     direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *     initialSortPriority: 1 // This field's sort order takes the second-highest priority.
   *   },
   *   {
   *     type: "field",
   *     fieldName: "NAME",
   *     label: "Name",
   *     direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *     initialSortPriority: 0 // This field's sort order takes the highest priority
   *    },
   *    {
   *      type: "field",
   *      fieldName: "STATUS",
   *      label: "Status",
   *      direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *      initialSortPriority: 2 // This field's sort order is prioritized after Name and ObjectId, respectively.
   *    }]
   * });
   */
  tableTemplate?: TableTemplateProperties | null;
  /**
   * The [TimeExtent](https://developers.arcgis.com/javascript/latest/references/core/time/TimeExtent/) in which to filter and display data within the FeatureTable widget. Setting this property directly on the widget or its [viewModel](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FeatureTableViewModel/#timeExtent) takes precedence over the layer's [FeatureLayer.timeExtent](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#timeExtent). If this property is set directly on the widget, the table will not refresh when the layer's [TimeExtent](https://developers.arcgis.com/javascript/latest/references/core/time/TimeExtent/) changes.
   *
   * @since 4.30
   * @see [FeatureLayer.timeExtent](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#timeExtent)
   * @example
   * // Filters the table to display only features that fit within the time extent
   * reactiveUtils.watch(
   *   () => timeSlider.timeExtent,
   *   (extent) => {
   *     featureTable.timeExtent = extent;
   *   }
   * );
   */
  timeExtent?: TimeExtentProperties | null;
  /** The view model for this widget. This is a class that contains all the logic, (ie. properties and methods), that controls the widget's behavior. See the [FeatureTableViewModel](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FeatureTableViewModel/) class to access all properties and methods on the widget. */
  viewModel?: FeatureTableViewModelProperties;
  /**
   * The visible elements that are displayed within the widget.
   * This property provides the ability to turn individual elements of the widget's display on/off.
   */
  visibleElements?: VisibleElementsProperties;
}

/** A custom function used to replace the title for the FeatureTable. This is useful in situations where it may be necessary to dynamically update titles based on the current state of the table. */
export type FeatureTableTitleFunction = () => string;

/** A custom function used to replace the description for the FeatureTable. This is useful in situations where it may be necessary to dynamically update descriptions based on the current state of the table. */
export type FeatureTableDescriptionFunction = () => string;

export interface FeatureTableEvents extends FeatureTableViewModelEvents {}

/**
 * The FeatureTable's functionality is described in the following sections:
 *
 * * [Overview](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#overview)
 * * [Selection and highlighting](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#selection)
 * * [Filtering and sorting](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filtering-and-sorting)
 * * [Editing within the FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editing-in-featuretable)
 * * [Related records](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#related-records)
 * * [Configuring columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#column-configuration)
 * * [Custom columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#custom-columns)
 * * [Menu and menu items](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#menu-configuration)
 *
 * <span id="overview"></span>
 * ## Overview
 * The `FeatureTable` widget provides an interactive tabular view of each feature's attributes in a layer. In addition, it also works with standalone tables that are not associated with underlying geometries. The FeatureTable supports the following layer types:
 *
 * * [FeatureLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/)
 * * [GeoJSONLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/GeoJSONLayer/)
 * * [CSVLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/CSVLayer/)
 * * [ImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ImageryLayer/) - Take note that it must have a mosaic dataset. Currently, `Map` and `FeatureTable` interaction with `ImageryLayers` are not supported.
 * * [WFSLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/WFSLayer/)
 * * [SceneLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/SceneLayer/) (with an associated FeatureLayer)
 * * [KnowledgeGraphSublayer](https://developers.arcgis.com/javascript/latest/references/core/layers/knowledgeGraph/KnowledgeGraphSublayer/)
 * * [CatalogFootprintLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/catalog/CatalogFootprintLayer/)
 * * [MapImageLayer.sublayers](https://developers.arcgis.com/javascript/latest/references/core/layers/MapImageLayer/#sublayers)
 *
 * The FeatureTable varies slightly from other widgets in that it can work independently of a map or scene view. This means that the FeatureTable can be used in a standalone application or in a view that does not contain a map or scene. However, if the FeatureTable is used in conjunction with a map or scene view, the view must be set on the FeatureTable in order for the [selection and highlighting](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#selection) to work. Implementing the FeatureTable programmatically is also slightly different than other widgets in that it is designed to be added to an application via its [container](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#container) element, as opposed to being added via the default UI, ie. `view.ui.add()`. The following example demonstrates how to add the FeatureTable to an application:
 *
 * ```javascript
 * const featureTable = new FeatureTable({
 *  view: view,
 * layer: featureLayer,
 * container: "tableDiv"
 * });
 * ```
 *
 * The FeatureTable can be configured to display specific columns (including attachments), filter features, and sort columns. It provides the ability
 * to [edit](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editing-in-featuretable) feature attributes, [select and highlight](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#selection) features within the table and its associated view. In addition, it can also display and edit [related records](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#related-records). The latter beginning with version 4.30. The following sections provide more detail on how to configure and use the FeatureTable.
 *
 *  <span id="selection"></span>
 * ## Selection and highlighting
 * The FeatureTable allows manual interaction or programmatic control over selecting and
 * highlighting features within both the table and its associated view.
 *
 * To enable manual-driven selection and highlighting, ensure the table's [view](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#view) and [highlightEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightEnabled) properties are configured.
 * Once set, interacting with a table row will select and highlight the corresponding feature in the view.
 * Multiple selections within the table are turned on by default. To update this behavior, set the [multipleSelectionEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#multipleSelectionEnabled) property to `false`.
 *
 * ![featuretable-highlightIds](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/selected-features.png)
 *
 * <details>
 * <summary>Read More</summary>
 *
 * If you wish to independently wire up selection and highlighting, set the
 * table's [highlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightIds). Modifying this collection by adding or removing feature object IDs
 * will automatically update the selection status of the corresponding rows in the table. To deselect all
 * features, simply clear the collection.
 *
 * Highlighting rows within the table can be accomplished using [rowHighlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#rowHighlightIds). This property allows for the visual differentiation of rows based on criteria such as specific values or edits, without affecting their selection status. For example, you can use it to highlight rows that contain a specific value or rows that have been edited.
 *
 * ```javascript
 * // This snippet highlights rows within the table that have been edited.
 * featureTable.layer.on("edits", (event) => {
 *   event.updatedFeatures.forEach((feature) => {
 *     featureTable.rowHighlightIds.push(feature.getObjectId());
 *   });
 * });
 * ```
 * ![featuretable-rowHighlightIds](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/rowhighlightids.png)
 *
 * Additionally, selection and highlight behavior can be enhanced using the feature table's [events](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#FeatureTableEvents). These events are designed to listen for cell interaction and include: [@cell-click](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#event-cell-click), [@cell-keydown](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#event-cell-keydown), [@cell-pointerout](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#event-cell-pointerout), and [@cell-pointerover](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#event-cell-pointerover). The latter two events are especially useful for tasks such as highlighting a feature in the view while hovering over a cell, all doing so without requiring the end-user to select the row. For example, these events can be used to display cell information in a tooltip that is attached to the mouse cursor.
 *
 * > In short, the [highlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightIds) property is used to control and synchronize selections between the FeatureTable and other components of the SDK, whereas [rowHighlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#rowHighlightIds) is for adding a highlight to some rows, based on some other interaction within the application. It allows the ability to select specific rows based on actions performed in the application, such as hovering over a feature on the map or applying a filter through another widget.
 *
 * </details>
 *
 * <span id="filtering-and-sorting"></span>
 * ## Filtering and sorting
 * The FeatureTable provides the ability to filter and sort features within the table. A few different approaches to filtering are outlined in the section below.
 *
 * <details>
 * <summary>Read More</summary>
 *
 * * **Filtering by selection**- By default, the table displays all rows, regardless of their selection status. However, if needing to display only selected rows, set the [filterBySelectionEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterBySelectionEnabled) property to `true`. When set, the table will only display rows for selected features. This is useful for focusing on a specific subset of features. Setting the FeatureTable's [objectIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#objectIds) performs similar functionality where only the set object IDs will display within the table. If the collection is empty, all features display. The difference between [filterBySelectionEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterBySelectionEnabled) and [objectIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#objectIds) is the latter does not require a selection. Rather, it only displays features with a matching object ID, whereas setting [filterBySelectionEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterBySelectionEnabled) indicates to the table that it should only display features that are already selected. This selection is handled by passing in a collection of object IDs to the table's [highlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightIds). Setting this property selects the corresponding features within the table while highlighting them in the view. For an example of how to filter using these properties, see the sample [FeatureTable with related records](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/).
 * * **Filtering by geometry**- This is done by setting the [filterGeometry](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterGeometry) property to a geometry. When set, the table will only display rows for features that intersect the geometry. This is useful for focusing on a specific subset of features that intersect a geometry. See the samples, [FeatureTable with a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/), and [FeatureTable with related records](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)) for an example of how to filter features by geometry.
 * * **Filtering by time**- To achieve this, configure the [timeExtent](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#timeExtent) property to a time extent. Once configured, the table will exclusively show rows corresponding to features that are within the defined time extent.
 * **Filtering by selection and geometry**- This is accomplished by setting both the [objectIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#objectIds) and [filterGeometry](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterGeometry) properties. When set, the table will only display rows for features that match the corresponding object ID and and intersect the geometry.
 *
 * It is possible to control sort order individually per column or on multiple columns. The latter is accomplished by setting the [multiSortEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#multiSortEnabled) property to `true`. When set, the table will allow sorting on multiple columns. The table's [sortColumn()](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#sortColumn) method is used to sort columns. This method takes the field name and sort order as parameters. The sort order can be set to either `asc` or `desc`. The priority of columns and their sort order can be set using [FieldColumnTemplate.initialSortPriority](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#initialSortPriority). This property is used to set the initial sort priority for a column.
 *
 * > [!WARNING]
 * >
 * >  If the table is in its default sort mode with no initial sort order provided, the first field on the layer automatically sorts in ascending order.
 *
 * </details>
 *
 * <span id="editing-in-featuretable"></span>
 * ## Editing within the FeatureTable
 * The FeatureTable provides the ability to edit feature attributes within the table. This is accomplished by setting the [editingEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editingEnabled) property to `true`. The editing experience is similar to that of a spreadsheet, where end-users can click into a cell to edit its value. The table automatically saves edits to the layer once navigating away from the cell or `Enter` is pressed. Press the `Esc` key to cancel an edit. The FeatureTable also provides the ability to delete selected records. This is done by selecting the rows to delete and clicking the `Delete Selection` menu item. The `Delete Selection` menu item is only available if the [editingEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editingEnabled) property is set to `true` and the feature service supports editing.
 *
 * ![featuretable editing](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/editing.png)
 *
 * <details>
 * <summary>Read More</summary>
 *
 * Editing permissions can be separated into the following levels of priority:
 * * [FeatureLayer.editingEnabled](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#editingEnabled) - This is derived from the layer's [FeatureLayer.editingEnabled](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#editingEnabled) property. This must always be `true` for editing to be enabled.
 * * [Field](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Field/) - This is derived from the [FeatureLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/). It takes what is set in the [Field.editable](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Field/#editable)
 * property. This must always be `true` for editing to be enabled, although it can be overridden to `false` (not
 * vice-versa) via a [field column template](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#editable).
 * * [FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/) - The [editingEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editingEnabled) property must be set on the table in order for any type of editing to be enabled.
 * * [Template](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#editable) - The editable permissions on a field can be configured by setting the [FieldColumnTemplate.editable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#editable) property of the [FieldColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/).
 *
 * > [!WARNING]
 * >
 * > If the service's field is not editable, it is not possible to override its permissions using any of the options above. [FieldColumnTemplate.editable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#editable) can never override what is set on the layer, field, or table.
 * > Editing is disabled for big integer fields that contain a value that falls outside of the max and min whole number threshold of -9007199254740991 and 9007199254740991. Any attempts to edit value higher or lower than these thresholds will not work and those edits will not be saved.
 * > An Object ID field is required for editing to work.
 *
 * </details>
 *
 * <span id="related-records"></span>
 * ## Related records
 * Beginning with version 4.30, the FeatureTable supports the ability to display and edit related records within the table.
 *
 *  | Origin table with related records |
 *  |---------------------------------- |
 *  | ![Origin table related records](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/relate-table-origin.png) |
 *
 *  | Destination table with related records |
 *  |-----------------------------------|
 *  | ![Destination table related records](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/relate-dest-table.png) |
 *
 * This is done by setting the [relatedRecordsEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#relatedRecordsEnabled) property to `true`. When this property is set, the table will contain a field that links to any related records associated with each feature in the table. When clicked, a separate table displays the related records for that feature. The FeatureTable provides the ability to show nested related records, which are related records that contain their own related records. It automatically recognizes if there are any related tables associated a table and, if applicable, displays links to related records for that feature or row. The following example demonstrates how to configure the FeatureTable to display related records:
 *
 * ```javascript
 * const featureTable = new FeatureTable({
 *   view: view,
 *   layer: featureLayer,
 *   relatedRecordsEnabled: true,
 *   container: "tableDiv"
 * });
 * ```
 * <details>
 * <summary>Read More</summary>
 *
 * The main difference with how related records work in comparison to the Editor widget is that the FeatureTable does not have to have a template configured with relationship information for it to be recognized and automatically picked up within the table. In order for related data to display within the table, the [relatedRecordsEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#relatedRecordsEnabled) property must be set to `true`, the origin table must have an associated relationship class, and its related data must also be contained within the map. If these conditions are met, any related records associated with the table's features will automatically display with embedded links to its related data. The FeatureTable also provides the ability to show nested related records, which are related records that contain their own related records. This is useful for displaying complex relationships within the table.
 *
 * There are a couple of known limits when working with relationships in the FeatureTable:
 *
 * * Displaying and editing related records is only supported using ArcGIS Online and ArcGIS Enterprise version 11.2 or higher feature services.
 * * Editing related records is only supported if the related table is editable.
 * * The related layers or tables must also be added to the map to be able to display related records within the table.
 *
 * </details>
 *
 * <span id="column-configuration"></span>
 * ## Configuring columns
 * The FeatureTable provides various column types that can be configured to display within the table.
 *
 * These column types include:
 * * `column` ([Column](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/Column/)) - This is the column type that supports the underlying functionality for the various column types listed below. Use this column type to create custom columns that display within the table and are not directly tied to data within a layer.
 * * `field` ([FieldColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FieldColumn/)) - This is the column type that displays the field name and value within the table. This column type is the type used to display the field name and value for features within the table.
 * * `action` ([ActionColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/ActionColumn/)) - This is the column type that displays [action](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#actionColumnConfig) buttons within the table.
 * * `relationship` ([RelationshipColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/RelationshipColumn/)) - This is the column type that displays [related records](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#related-records) within the table.
 * * `attachment` ([AttachmentsColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/AttachmentsColumn/)) - This is the column type that displays [attachments](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attachmentsEnabled) within the table. Take note that viewing attachments directly within the table's cell is not supported, although if a feature contains attachments, the total count per feature will display.
 * * `group` ([GroupColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/GroupColumn/)) - This is the column type that groups columns within the table.
 *
 * <details>
 * <summary>Read More</summary>
 *
 * These column classes contain information about the current table state. And although it is possible to interact directly with these columns, ie. updating properties, calling methods, etc., the recommended approach is to set the [tableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableTemplate) with configured column templates prior to table creation. The reason for this is because templates are designed to help configure columns prior to table creation and therefore wil not update based on the table's underlying state changes.
 *
 * At version 4.30, the FeatureTable's API has been improved to not regenerate columns unless in only very necessary cases, e.g. setting an entirely new [tableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableTemplate) and forcing the table to refresh. Although this approach is supported, it may not be necessary as it is possible to directly interact with the underlying columns and not cause unnecessary re-renders.
 *
 * The column configuration of the FeatureTable is designed to be flexible, providing a rich user experience. While it is generally recommended to update the corresponding template when changes are made to a column's property, it is not mandatory if there is no need to regenerate columns forcefully.
 *
 * > [!WARNING]
 * >
 * > There are some properties pertaining to width and alignment that may not function as expected if modified dynamically.
 *
 * * The [ColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/ColumnTemplate/) class can be used to create and configure custom columns that display within the table.
 * * The [FieldColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/) class is tied to a layer and is used to configure and display field names and values within the table.
 * * Lastly, the [GroupColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/GroupColumnTemplate/) class is used to configure grouped columns within the table. Each template contains the field name, label, and other properties that define the column's behavior. The following example demonstrates how to configure the field columns within the FeatureTable's table template:
 *
 *  ```javascript
 * const featureTable = new FeatureTable({
 *   view: view,
 *   layer: featureLayer,
 *   tableTemplate: ({  // autocastable to table template
 *     columnTemplates: [{
 *       type: "field", // autocastable to field column template
 *       fieldName: "field1",
 *       label: "Field 1"
 *     },
 *     {
 *       type: "field",
 *       fieldName: "field2",
 *       label: "Field 2"
 *     }]
 *   }),
 *   container: "tableDiv"
 * });
 * ```
 * The template classes are designed primarily to configure columns prior to table creation. Once the table is created, the templates will not update based on state changes. The underlying column classes have information about the current table state.
 *
 * </details>
 *
 * <span id="custom-columns"></span>
 * ### Custom columns
 * Beginning with version 4.30, support for custom columns, ie. virtual columns, was added. Custom columns can be configured and displayed by leveraging the [ColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/ColumnTemplate/) class. The following example demonstrates how to create a custom column that displays a rating of stars within the table based on a feature's rating. It does so using a calcite component that displays the rating as stars. In addition, it makes use of the column template's [ColumnTemplate.formatFunction](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/ColumnTemplate/#formatFunction) property. This function is useful as it allows for dynamic content to be displayed within the table's cell and provides a way to add a rich user experience to the table.
 *
 * <details>
 * <summary>Read More</summary>
 *
 * [![FeatureTable custom content](https://developers.arcgis.com/javascript/latest/assets/images/guide/whats-new/430/featuretable/table-custom-content.png)](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-custom-content/)
 *
 *
 * ```javascript
 * const columnConfig = new ColumnTemplate({
 *   fieldName: "rating_stars",
 *   icon: "inspection",
 *   label: "Average Rating",
 *   formatFunction: ({ feature, index }) => {
 *     // Reuse existing components for optimal performance
 *     let component = ratingComponentMap.get(index);
 *     if (component) {
 *       return component;
 *     }
 *     component = document.createElement("calcite-rating");
 *     component.readOnly = true;
 *     component.value = Math.round(Math.random() * 5);
 *     ratingComponentMap.set(index, component);
 *     return component;
 *   }
 * });
 * ```
 *
 * </details>
 * <span id="menu-configuration"></span>
 * ## Menu and menu items
 * The FeatureTable provides functionality in the form of menu items within the FeatureTable's menu. These items perform various actions such as clearing the selection, deleting selected records, showing selected records, refreshing the data, and zooming to selected records.
 *
 * <details>
 * <summary>Read More</summary>
 *
 * The default menu items can be configured to be hidden, disabled, or selected by default.
 * The menu items can also be configured to include customized functionality. This is handled via the table's [menuConfig](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#menuConfig) property. This property takes an object where custom menu items are set within the table's menu. This object contains an array of menu items that define the label, icon, and click function for each item and whether these items should display by default.
 *
 * If you want to clear the selection, you can use the `clearSelection` menu item provided by the API. If you want to delete selected records, you can use the `deleteSelection` menu item provided by the API. The following example demonstrates how to configure the menu items with similar functionality but with different icons. The default menu items are not hidden and the custom menu items are appended to the menu items. If you wish to hide the default menu items, you can set their visible element to `false`.
 *
 * | Default column menu items   | Configured column menu items |
 * |--------|------|
 * | ![Default column menu items](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/column-menu-items.png) | ![Configured column menu items](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/column-menu-items-config.png)) |
 *
 * ```javascript
 * featureTable.menuConfig = {
 *   items: [
 *     {
 *       label: "Clear selection",
 *       icon: "erase",
 *       clickFunction: () => {
 *         featureTable.highlightIds.removeAll();
 *       }
 *     },
 *   {
 *     label: "Delete selection",
 *     icon: "group-x",
 *     clickFunction: () => {
 *       featureTable.viewModel.deleteSelectedRecords();
 *     }
 *   }]
 * };
 * ```
 *
 * In addition to configuring the table's menu items, it is also possible to configure an individual column's menu items. This is handled via the [ColumnTemplate.menuConfig](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/ColumnTemplate/#menuConfig) property. This is similar to what can be configured for the table's [menuConfig](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#menuConfig), but also includes the ability to customize the column's menu icon, and the selection mode of the menu.
 *
 * The following is an example of how to configure individual menu items within the FeatureTable's column menu. Similar to how the default table menu items are configured, any custom menu items are appended to the menu items. If you wish to hide the default menu items, you can set their visible element to `false`.
 *
 * | Default table menu items   | Configured table menu items |
 * |--------|------|
 * | ![Default table menu items](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/table-menu-items.png) | ![Configured table menu items](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/table-menu-items-config.png)) |
 *
 * ```javascript
 * const columnTemplate = new FieldColumnTemplate({
 *   menuConfig: {
 *     items: [{
 *       label: "Sort Ascending",
 *       icon: "a-z-down",
 *       clickFunction: () => {
 *         featureTable.sortColumn(columnTemplate.fieldName, "asc");
 *       }
 *     },
 *     {
 *       label: "Sort Descending",
 *       icon: "a-z-up",
 *       clickFunction: () => {
 *         featureTable.sortColumn(columnTemplate.fieldName, "desc");
 *       }
 *     }]
 *    }
 * });
 * ```
 *
 * </details>
 *
 * @deprecated since version 5.0. Use the [Feature Table](https://developers.arcgis.com/javascript/latest/references/map-components/components/arcgis-feature-table/) component instead. For information on widget deprecation, read about [Esri's move to web components](https://developers.arcgis.com/javascript/latest/components-transition-plan/).
 * @since 4.15
 * @see [FeatureTableViewModel](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FeatureTableViewModel/)
 * @see [DefaultUI](https://developers.arcgis.com/javascript/latest/references/core/views/ui/DefaultUI/)
 * @see [FeatureLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/)
 * @see [Sample - FeatureTable](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable/)
 * @see [Sample - FeatureTable using a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/)
 * @see [Sample - FeatureTable with editing enabled](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-editing/)
 * @see [Sample - FeatureTable with popup interaction](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-popup-interaction/)
 * @see [Sample - FeatureTable with related records](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
 * @see [Sample - FeatureTable with custom content](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-custom-content/)
 * @see [Sample - FeatureTable with row highlight](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-row-highlights/)
 * @see [Highlight features by geometry sample](https://developers.arcgis.com/javascript/latest/sample-code/highlight-features-by-geometry/)
 */
export default class FeatureTable extends Widget<FeatureTableProperties> {
  /**
   * @deprecated
   * Do not directly reference this property.
   * Use EventNames and EventTypes helpers from \@arcgis/core/Evented
   */
  "@eventTypes": FeatureTableEvents;
  /**
   * @example
   * // Typical usage for the FeatureTable widget. This will recognize all fields in the layer if none are set.
   * const featureTable = new FeatureTable({
   *   view: view, // The view property must be set for the select/highlight to work
   *   layer: featureLayer,
   *   container: "tableDiv" // the table must be assigned to the container via the constructor
   * });
   */
  constructor(properties?: FeatureTableProperties);
  /**
   * Reference to the current [action column](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/ActionColumn/). This column is only generated if a [configuration](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#actionColumnConfig) has been provided.
   *
   * @since 4.33
   * @see [actionColumnConfig](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#actionColumnConfig)
   */
  get actionColumn(): ActionColumn | null | undefined;
  /**
   * Configuration for the [ActionColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/ActionColumn/).
   * This property allows for customizing the action column's appearance and behavior.
   * The action column is a column that contains interactive buttons for each row.
   * These buttons can be used to perform actions such as editing, deleting, or
   * viewing additional information about a feature. This column is displayed as the
   * last column in the table and is only displayed if this property is set.
   *
   * ![featuretable action column](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/action-column.png)
   *
   * @since 4.30
   * @see [actionColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#actionColumn)
   * @see [ActionColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/ActionColumn/)
   * @see [Calcite Icon Search](https://developers.arcgis.com/calcite-design-system/icons/)
   * @see [Sample - FeatureTable with row highlighting](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-row-highlights/)
   * @example
   * // The following snippet demonstrates how to configure an action column that adds a button
   * // to each row which allows an end-user to zoom to the associated row's feature within the view.
   * featureTable.actionColumnConfig = {
   *   label: "Go to feature",
   *    icon: "zoom-to-object",
   *    callback: (params) => {
   *     view.goTo(params.feature);
   *  }
   * }
   */
  accessor actionColumnConfig: ActionColumnConfig | null | undefined;
  /**
   * A read-only property indicating the type of filter used by the table. It returns either filters by geometry or selections using a row's object ID.
   *
   * @see [Sample - FeatureTable using a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/)
   */
  get activeFilters(): Collection<FeatureTableSupportedFilters>;
  /**
   * Use this read-only property if needing to query features while retaining a column's sort order. It returns an array of [ColumnSortOrder](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/types/#ColumnSortOrder) which contains a column's name and its sort direction. The sort priority is honored in the returned [ColumnSortOrder](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/types/#ColumnSortOrder) if [multiSortEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#multiSortEnabled) is `true` with a set [FieldColumnTemplate.initialSortPriority](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#initialSortPriority).
   *
   * @default []
   * @since 4.25
   */
  get activeSortOrders(): ColumnSortOrder[];
  /**
   * A flattened array of all [columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/Column/) within the table, including nested columns. Take note that [group columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/GroupColumn/) are separate entries. This property is useful when applying
   * updates to all columns, including nested columns, as it provides ease of use when iterating through all columns in the table.
   *
   * @since 4.33
   */
  get allColumns(): FeatureTableSupportedColumn[];
  /**
   * Indicates the table is displaying all related tables in `show all` mode.
   *
   * @since 4.31
   */
  get allRelatedTablesVisible(): boolean;
  /**
   * A flattened array of all visible [columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/Column/) within the table, including nested columns. [Group columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/GroupColumn/) are separate entries.
   *
   * @since 4.33
   */
  get allVisibleColumns(): FeatureTableSupportedColumn[];
  /**
   * A flattened array of all [attachment columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/AttachmentsColumn/) within the table, including nested columns.
   *
   * @since 4.33
   */
  get attachmentsColumns(): AttachmentsColumn[];
  /**
   * Indicates whether to display the `Attachments` field in the table. It displays the count of attachments per feature and is only applicable if the feature layer [supports attachments](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#capabilities).
   *
   * ![featuretable attachmentsEnabled](https://developers.arcgis.com/javascript/latest/assets/references/core/widgets/featuretable/attachments-enabled.png)
   *
   * @default false
   * @see [FeatureLayer.capabilities.data.supportsAttachment](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#capabilities)
   */
  accessor attachmentsEnabled: boolean;
  /**
   * This read-only property provides the the configuration options for the attachments view.
   *
   * @since 4.33
   */
  accessor attachmentsViewOptions: AttachmentsViewOptions;
  /**
   * Use this property to configure how columns display within the table in regard to visibility, column order, and sorting.
   *
   * > [!WARNING]
   * >
   * > This property differs from the [tableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableTemplate) property. The `tableTemplate` property provides more fine-grained control over how the table is rendered within the application by offering more advanced configurations such as custom cell rendering, column formatting, and more. It is useful for application-level development that remains within an application. Use the [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate) property to access the table's settings across different applications. By using this property, the settings can be saved within a webmap or layer. Please refer to the [AttributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/tables/AttributeTableTemplate/) and [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/) documentation for more information.
   *
   * @since 4.31
   * @see [Sample - FeatureTable Component](https://developers.arcgis.com/javascript/latest/sample-code/feature-table/)
   */
  get attributeTableTemplate(): AttributeTableTemplate | null | undefined;
  set attributeTableTemplate(value: AttributeTableTemplateProperties | null | undefined);
  /**
   * Indicates whether the table should automatically refresh when the underlying data changes. This property is useful when the table is displaying data from a feature layer that is being updated by other clients.
   *
   * @default true
   * @since 4.23
   */
  accessor autoRefreshEnabled: boolean;
  /**
   * Indicates whether the table should automatically save edits, or store all edits locally until the 'savePendingEdits' or 'discardPendingEdits' methods are called.
   *
   * @default true
   * @since 5.0
   */
  accessor autoSaveEnabled: boolean;
  /**
   * Indicates whether the table supports adding a related record based on the current relationship
   * configuration. This accounts for whether table is currently configured to show related features.
   * If the table is not displaying related features, then the value of this property is always false.
   *
   * @default false
   * @since 5.0
   */
  get canAddRelatedFeature(): boolean;
  /**
   * Indicates whether to enable the table's column performance mode. This mode is designed to improve the performance of the table when working with a large number of columns.
   *
   * @default true
   * @since 4.33
   */
  accessor columnPerformanceModeEnabled: boolean;
  /**
   * Indicates whether the table should allow reordering of columns.
   *
   * @default true
   * @since 4.16
   */
  accessor columnReorderingEnabled: boolean;
  /**
   * A read-only collection of [Column](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/Column/), [field](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FieldColumn/), [group](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/GroupColumn/), [action](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/ActionColumn/), [attachment](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/AttachmentsColumn/), and [relationship](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/RelationshipColumn/) columns that are displayed within the table.
   *
   * By default fields such as `CreationDate`, `Creator`, `EditDate`, `Editor`, and `GlobalID` do not show. If these fields are needed, set them via [TableTemplate.columnTemplates](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/#columnTemplates). In this scenario, it is also necessary to set the column template's `visible` property to `true`.
   */
  get columns(): Collection<FeatureTableSupportedColumn>;
  /**
   * The ID or node representing the DOM element containing the widget.
   * Take note that this property can only be set once. The snippets below provide some examples for setting this.
   *
   * > [!WARNING]
   * >
   * > The [FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/) widget's container property must be set within its constructor and not via the [view.ui.add()](https://developers.arcgis.com/javascript/latest/references/core/views/ui/UI/#add) method.
   *
   * @example
   * // Create the HTML div element programmatically at runtime and set to the widget's container
   * const featureTable = new FeatureTable({
   *   view: view,
   *   container: document.createElement("tableDiv")
   * });
   * @example
   * // Specify an already-defined HTML div element in the widget's container
   *
   * const featureTable = new FeatureTable({
   *   view: view,
   *   container: featureTableDiv
   * });
   *
   * // HTML markup
   * <body>
   *   <div id="viewDiv"></div>
   *   <div class="container">
   *     <div id="tableDiv"></div>
   *   </div>
   * </body>
   * @example
   * // Create the HTML div element programmatically at runtime and set to the widget's container
   * const basemapGallery = new BasemapGallery({
   *   view: view,
   *   container: document.createElement("div")
   * });
   *
   * // Add the widget to the top-right corner of the view
   * view.ui.add(basemapGallery, {
   *   position: "top-right"
   * });
   * @example
   * // Specify an already-defined HTML div element in the widget's container
   *
   * const basemapGallery = new BasemapGallery({
   *   view: view,
   *   container: basemapGalleryDiv
   * });
   *
   * // Add the widget to the top-right corner of the view
   * view.ui.add(basemapGallery, {
   *   position: "top-right"
   * });
   *
   * // HTML markup
   * <body>
   *   <div id="viewDiv"></div>
   *   <div id="basemapGalleryDiv"></div>
   * </body>
   * @example
   * // Specify the widget while adding to the view's UI
   * const basemapGallery = new BasemapGallery({
   *   view: view
   * });
   *
   * // Add the widget to the top-right corner of the view
   * view.ui.add(basemapGallery, {
   *   position: "top-right"
   * });
   */
  get container(): HTMLElement | null | undefined;
  set container(value: HTMLElement | string | null | undefined);
  /**
   * The SQL where clause used to filter features visible in the table. Only the features that satisfy the definition
   * expression are displayed in the table. This value takes priority over any definition expression set on the
   * associated layer.
   *
   * @since 4.33
   */
  accessor definitionExpression: string | null | undefined;
  /**
   * Text displayed in the table header, under the title. This can be a basic string or custom function that returns a string. This is useful in situations where additional context is needed for the table.
   *
   * @since 4.30
   */
  accessor description: FeatureTableDescriptionFunction | string | null | undefined;
  /**
   * Indicates whether the table is disabled. When disabled, the table will not display any data or allow interaction. This is purely a visual override and does not prevent the table from attempting to render data. This property has no effect on the current [state](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#state) property value.
   *
   * @default false
   * @since 4.30
   */
  accessor disabled: boolean;
  /**
   * Indicates whether [editing](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editing-in-featuretable) is enabled on the data within the feature table. Double-clicking in a cell will enable editing for that value. In order to edit a feature, the layer must be editable and the user must have the appropriate permissions. In addition, the layer must contain an object ID field.
   *
   * @default false
   * @since 4.16
   * @see [Editing within the FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editing-in-featuretable)
   * @see [Sample - FeatureTable with editing enabled](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-editing/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   */
  accessor editingEnabled: boolean;
  /**
   * The total number of records displayed in the table's current view. Normally, this is equivalent to [size](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#size) for default configurations.
   * If [paginationEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled) is true, `effectiveSize` reflects the total number of visible rows for the current page.
   * This value is usually equivalent to [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize) unless viewing the last page of data. The last page may display less features
   * than the maximum number for a single page. This property also takes into account all [active filters](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#activeFilters) and the current value
   * of [maxSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#maxSize).
   *
   * @since 4.31
   */
  get effectiveSize(): number;
  /**
   * A reference to the [FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/) instance used for menu actions.
   *
   * @since 5.0
   */
  get effectiveTable(): FeatureTable;
  /**
   * A flattened array of all [field columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FieldColumn/) within the table, including nested columns.
   *
   * @since 4.33
   */
  get fieldColumns(): FieldColumn[];
  /**
   * Indicates whether the table only displays rows that are considered selected. Row selection can be modified by adding or removing associated object IDs from `highlightIds`. This will cause the store to fetch fresh features to ensure the expected sort order is honored.
   *
   * @default false
   * @since 4.30
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   */
  accessor filterBySelectionEnabled: boolean;
  /**
   * Set this property to filter the features displayed in the table. It accepts a [Geometry](https://developers.arcgis.com/javascript/latest/references/core/geometry/Geometry/), e.g. [Extent](https://developers.arcgis.com/javascript/latest/references/core/geometry/Extent/), and uses it as a spatial filter. When modifying this property, the `FeatureTable` will completely refresh and re-query for all features.
   *
   * @since 4.19
   * @see [Sample - FeatureTable using a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @example
   * // Listen for when the view is stationary.
   * // If true, check the view's extent and set
   * // the table to display only the attributes
   * // for the features falling within this extent.
   *
   * reactiveUtils.when( () => view.stationary === true,
   * () => {
   *   // Get the new extent of view/map whenever map is updated.
   *   if (view.extent) {
   *     // Filter out and show only the visible features in the feature table.
   *     featureTable.filterGeometry = view.extent;
   *   }
   * }, { initial: true });
   */
  get filterGeometry(): GeometryUnion | null | undefined;
  set filterGeometry(value: ((ExtentProperties & { type: "extent" }) | (MultipointProperties & { type: "multipoint" }) | (PointProperties & { type: "point" }) | (PolygonProperties & { type: "polygon" }) | (PolylineProperties & { type: "polyline" }) | (MeshProperties & { type: "mesh" })) | null | undefined);
  /**
   * A flattened array of all [group columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/Grid/GroupColumn/) within the table.
   *
   * @since 4.33
   */
  get groupColumns(): GroupColumn[];
  /**
   * Convenience property indicating the data set contains contingent values. This includes contingent values in a related table.
   *
   * @default false
   * @since 5.0
   */
  get hasContingentValues(): boolean;
  /**
   * Indicates there are unsaved edits and that they are invalid. This includes invalid pending edits in a related table.
   *
   * @default false
   * @since 5.0
   */
  get hasInvalidPendingEdits(): boolean;
  /**
   * Indicates there are unsaved edits. This includes edits in a related table.
   * This does not account for whether the edits are valid or not.
   *
   * @default false
   * @since 5.0
   */
  get hasPendingEdits(): boolean;
  /**
   * A collection of string values which indicate [field.names](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Field/) that should be hidden within the table. By default fields such as `CreationDate`, `Creator`, `EditDate`, `Editor`, and `GlobalID` do not show. If these fields are needed, set them via [TableTemplate.columnTemplates](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/#columnTemplates). In this case, it is required to set column template's [FieldColumnTemplate.visible](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#visible) property to `true`.
   *
   * @since 4.16
   * @see [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/)
   * @see [FieldColumnTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/)
   * @example
   * const featureTable = new FeatureTable({
   *   layer: featureLayer,
   *   view: view,
   *   hiddenFields: ["Primary_Type", "incident_date"], // will not show these two fields within the table
   *   container: document.getElementById("tableDiv")
   * });
   * @example
   * // Set this syntax if needing to display a default hidden field, e.g. 'CreationDate`
   * const featureTable = new FeatureTable({
   *   layer: featureLayer,
   *   view: view,
   *   tableTemplate: { // autocastable to TableTemplate
   *     columnTemplates: [ // takes an array of FieldColumnTemplate and GroupColumnTemplate
   *     { //autocastable to FieldColumnTemplate
   *       type: "field",
   *       fieldName: "date_created",
   *       label: "Date created",
   *       visible: true
   *     }]
   *   }
   *   container: document.getElementById("tableDiv")
   * });
   */
  get hiddenFields(): Collection<string>;
  set hiddenFields(value: ReadonlyArrayOrCollection<string>);
  /**
   * Indicates whether to highlight the associated feature when a row is selected by checking the corresponding checkbox. This property is only applicable when working with a map and the [view](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#view) property is set.
   * This property has no effect if [syncViewSelection](#syncViewSelection) is `true` or a custom [selectionManager](#selectionManager) has been provided.
   *
   * @default true
   * @since 4.25
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @see [Sample - FeatureTable with popup interaction](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-popup-interaction/)
   */
  accessor highlightEnabled: boolean;
  /**
   * This property accepts and returns a collection of feature object IDs. Use this to access and control which features are currently selected in the table and subsequently highlighted within the map. Once an application sets a collection of object IDs, the table will select the corresponding row and highlight its feature within the map.
   *
   * @since 4.25
   * @see [highlightEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightEnabled)
   * @see [Sample - FeatureTable using a map](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-map/)
   * @see [Sample - FeatureTable with popup interaction](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-popup-interaction/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @see [Sample - FeatureTable with row highlighting](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-row-highlights/)
   * @example
   * // This example instantiates the table with highlighted features
   * const featureTable = new FeatureTable({
   *   view: view,
   *   layer: featureLayer,
   *   container: "tableDiv",
   *   highlightIds
   * });
   *
   * // Push the object IDs into a collection and select
   * featureTable.highlightIds.push(2, 3, 4);
   *
   * // Listen for changes in the collection of highlighted features
   * featureTable.highlightIds.on("change", (event) => {
   *   console.log("features selected", event.added);
   *   console.log("features deselected", event.removed);
   * });
   */
  get highlightIds(): Collection<ObjectId>;
  set highlightIds(value: ReadonlyArrayOrCollection<ObjectId>);
  /**
   * The string value which indicates the displayed [icon](https://developers.arcgis.com/calcite-design-system/icons/).
   *
   * @default "table"
   * @since 4.27
   * @see [Calcite Icon Search](https://developers.arcgis.com/calcite-design-system/icons/)
   * @see [Sample - FeatureTable with related data](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-relates/)
   * @see [Sample - FeatureTable with custom content](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-custom-content/)
   * @see [Calcite Icon Search](https://developers.arcgis.com/calcite-design-system/icons/)
   */
  get icon(): Icon["icon"];
  set icon(value: Icon["icon"] | null | undefined);
  /**
   * The user-provided number of total features accessed from the data source. This is used for initial load of the data store as opposed to querying a specified layer. Additionally, the table will query data counts to verify the data's integrity, or when requested via [refresh()](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#refresh).
   *
   * @since 4.31
   */
  accessor initialSize: number | null | undefined;
  /**
   * Indicates the table is querying or syncing data. This is useful when determining if the table is busy. This can be especially helpful when the table is querying a large amount of features.
   *
   * @since 4.31
   */
  get isQueryingOrSyncing(): boolean;
  /**
   * Indicates the table is syncing data. This is useful when determining if the table is busy.
   *
   * @since 5.0
   */
  get isSyncing(): boolean;
  /**
   * Indicates the table is syncing attachment edits.
   *
   * @since 4.31
   */
  get isSyncingAttachments(): boolean;
  /**
   * The widget's default label.
   *
   * @since 4.11
   */
  get label(): string;
  set label(value: string | null | undefined);
  /**
   * The associated [CatalogFootprintLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/catalog/CatalogFootprintLayer/), [CSVLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/CSVLayer/), [FeatureLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/), [GeoJSONLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/GeoJSONLayer/), [ImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ImageryLayer/),
   * [KnowledgeGraphSublayer](https://developers.arcgis.com/javascript/latest/references/core/layers/knowledgeGraph/KnowledgeGraphSublayer/), [ParquetLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ParquetLayer/) [SceneLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/SceneLayer/), [WFSLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/WFSLayer/), [OrientedImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/)
   * or [Sublayer](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Sublayer/) containing the fields and attributes to display within the widget.
   *
   * > [!WARNING]
   * >
   * > The table's pagination defaults to `50` records at a time. If the layer contains less than 50 records, it will use whatever count it has. Note that 0 records do not apply.
   * > Support for [CatalogFootprintLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/catalog/CatalogFootprintLayer/) was added at version 4.30.
   * > Support for [ParquetLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ParquetLayer/) was added at version 4.34. Column sorting is not supported for ParquetLayer.
   * > Support for [GeoJSONLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/GeoJSONLayer/), [CSVLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/CSVLayer/), [ImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ImageryLayer/), and [WFSLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/WFSLayer/) was added at version 4.21.
   * > For an [ImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ImageryLayer/) to work with FeatureTable, it must have a mosaic dataset.
   *
   * @see [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize)
   */
  accessor layer: FeatureTableSupportedLayer | null | undefined;
  /**
   * An array of layers listed within the [dropdown component](https://developers.arcgis.com/calcite-design-system/components/dropdown/) of the table's header. This component allows changing which table layer it should display. Typically, the dropdown component will display all supported layers within the [view](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#view) unless a specific set of layers is set. The table supports [CatalogFootprintLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/catalog/CatalogFootprintLayer/), [CSVLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/CSVLayer/), [FeatureLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/), [GeoJSONLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/GeoJSONLayer/), [ImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/ImageryLayer/), [KnowledgeGraphSublayer](https://developers.arcgis.com/javascript/latest/references/core/layers/knowledgeGraph/KnowledgeGraphSublayer/), [SceneLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/SceneLayer/), and [WFSLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/WFSLayer/) layers.
   *
   * @since 4.30
   */
  accessor layers: FeatureTableSupportedLayer[] | null | undefined;
  /**
   * The [layer view](https://developers.arcgis.com/javascript/latest/references/core/views/layers/LayerView/) associated with the table's [layer](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#layer).
   *
   * @since 4.30
   * @see [Sample - FeatureTable with row highlighting](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-row-highlights/)
   */
  get layerView(): LayerView | null | undefined;
  /**
   * This property is applicable when working with layers that contain a large number of features, as it provides the ability to limit the displayed total feature count. If [paginationEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled) is `true`, and `maxSize` is greater than the current value of [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize), multiple pages usually display. If `maxSize` is less than [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize), a single page shows the total number of features limited to `maxSize`.
   *
   * @since 4.31
   */
  accessor maxSize: number | null | undefined;
  /**
   * Set this object to customize the feature table's menu content.
   *
   * @since 4.16
   */
  accessor menuConfig: TableMenuConfig | null | undefined;
  /**
   * Controls whether the table allows multiple selected rows.
   *
   * @default true
   * @since 4.30
   */
  accessor multipleSelectionEnabled: boolean;
  /**
   * This property can be used to determine how newly sorted columns are prioritized. By default, newly sorted columns are
   * given the highest sort priority.
   *
   * @default "prepend"
   * @since 4.33
   */
  accessor multipleSortPriority: GridMultiSortPriority;
  /**
   * Indicates whether sorting multiple columns is supported within the table. Use this in combination with the [FieldColumnTemplate.initialSortPriority](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#initialSortPriority) and [FieldColumnTemplate.direction](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#direction) properties to set sorting functionality for multiple columns.
   *
   * @default false
   * @since 4.23
   * @see [FieldColumnTemplate.initialSortPriority](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#initialSortPriority)
   * @see [FieldColumnTemplate.direction](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#direction)
   * @see [sortColumn()](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#sortColumn)
   * @see [Sample - FeatureTable with editing enabled](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable-editing/)
   * @see [Sample - FeatureTable](https://developers.arcgis.com/javascript/latest/sample-code/widgets-featuretable/)
   * @example
   * const featureTableVM = new FeatureTable({
   *   layer: featureLayer,
   *   multiSortEnabled: true,
   *   tableTemplate: { // autocastable to TableTemplate
   *     columnTemplates: [ // takes an array of FieldColumnTemplate and GroupColumnTemplate
   *     { // autocastable to FieldColumnTemplate
   *       type: "field",
   *       fieldName: "ObjectId",
   *       direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *       initialSortPriority: 1, // This field's sort order takes second-highest priority.
   *     },
   *     {
   *       type: "field",
   *       fieldName: "Name",
   *       direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *       initialSortPriority: 0, // This field's sort order takes the highest priority.
   *     },
   *     {
   *       type: "field",
   *       fieldName: "Status",
   *       direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *       initialSortPriority: 2 // This field's sort order is prioritized after Name and ObjectId, respectively.
   *     }]
   *   },
   *   container: "tableDiv"
   * });
   */
  accessor multiSortEnabled: boolean;
  /**
   * This property controls the scale of all components in the navigation bar displayed when
   * viewing attachments or related records.
   *
   * @default "m"
   * @since 4.33
   */
  accessor navigationScale: "s" | "m" | "l";
  /**
   * This property can be used to override the text displayed when the table is fully loaded but no rows are available.
   *
   * @since 4.33
   */
  accessor noDataMessage: string | null | undefined;
  /**
   * This property accepts and returns a collection of feature object IDs. Use this to access and control which features are currently visible in the table.
   * When the collection is empty, all potential rows are displayed. Modifying object IDs is not supported while [filterBySelectionEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterBySelectionEnabled) is `true` as these properties are mutually exclusive.
   * This filter can also be combined with [filterGeometry](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#filterGeometry) to display features that satisfy both conditions.
   *
   * @since 4.30
   */
  get objectIds(): Collection<ObjectId>;
  set objectIds(value: ReadonlyArrayOrCollection<ObjectId>);
  /**
   * An array of field names from the table's data source to include when the table requests data. By default, all fields are requested. This property is useful when working with many fields and only a subset of them is needed for the table. Take note that doing so can improve the table's load time.
   *
   * @since 4.31
   */
  accessor outFields: string[] | null | undefined;
  /**
   * Number of pages of features to be displayed in the table, based on the total [number of features](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#size) and configured [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize).
   *
   * @since 4.31
   */
  get pageCount(): number;
  /**
   * Represents the index of the page of the feature currently being displayed. The number of features per page can be adjusted by modifying the [pageSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageSize). [Pagination](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled) must be enabled or the value of this property may not be not reliable due to virtualization of visible pages.
   *
   * @since 4.31
   */
  get pageIndex(): number;
  set pageIndex(value: number | number);
  /**
   * The default page size used when displaying features within the table. By default, the page loads the first 50 features returned by the service. It can be used with [paginationEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled) to display a subset of features.
   *
   * > [!WARNING]
   * >
   * > It is not possible to overwrite the maximum page size on the server, ie. `maxRecordCount`, as this property only applies to set values less than the maximum page size, i.e. `maxRecordCount`, set on the service.
   *
   * @default 50
   * @since 4.19
   * @see [ArcGIS REST API - FeatureLayer - maxRecordCount](https://developers.arcgis.com/rest/services-reference/feature-layer.htm)
   */
  accessor pageSize: number;
  /**
   * Controls whether the table should only display a single page of features at any time. Current page can be determined via the [pageIndex](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageIndex) property. The current page can be modified by calling the [goToPage()](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#goToPage) method and passing in the desired [page index](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageIndex).
   *
   * @default false
   * @since 4.31
   */
  accessor paginationEnabled: boolean;
  /**
   * Total number of individual cells with pending edits.
   *
   * @since 5.0
   */
  get pendingEditsCount(): number;
  /**
   * Indicates whether to display any related records associated with rows within the table. Take note that related layers must be provided via the [layers](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#layers) property when there is no associated [view](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#view).
   *
   * @default false
   * @since 4.30
   * @see [FeatureLayer.capabilities.data.supportsRelationships](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#capabilities)
   * @see [FeatureLayer.relationships](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#relationships)
   * @see [RelationshipColumn](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/RelationshipColumn/)
   */
  accessor relatedRecordsEnabled: boolean;
  /**
   * A nested table instance which represents a relationship to another table. This is a reference to the most recently generated table when multiple related tables exist. It is only applicable if the table instance manages all nested tables.
   *
   * @since 4.31
   */
  get relatedTable(): FeatureTable | null | undefined;
  /**
   * Indicates a nested table has contingent values.
   *
   * @since 5.0
   */
  get relatedTableHasContingentValues(): boolean;
  /**
   * Indicates a nested table has invalid pending edits.
   *
   * @since 5.0
   */
  get relatedTableHasInvalidPendingEdits(): boolean;
  /**
   * Indicates a nested table has pending edits.
   *
   * @since 5.0
   */
  get relatedTableHasPendingEdits(): boolean;
  /**
   * A collection of nested table instances. This is typically used to represent relationships between each other.
   * These are configured and managed by a single table widget instance. Only applies if this particular table instance is responsible for managing all nested tables.
   * Nested tables reference the main table controller via the [tableController](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableController) property.
   *
   * @since 4.31
   */
  get relatedTables(): Collection<FeatureTable>;
  /**
   * Reference to the relationship represented by the table.
   *
   * @since 5.0
   */
  get relationship(): Relationship | null | undefined;
  /**
   * A flattened array of all [relationship columns](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/RelationshipColumn/) within the table, including nested columns.
   *
   * @since 4.33
   */
  get relationshipColumns(): RelationshipColumn[];
  /**
   * Indicates whether to fetch geometries for the corresponding features displayed in the table.
   *
   * > [!WARNING]
   * >
   * > Setting this to `true` can potentially impact the widget's performance.
   *
   * @default false
   * @since 4.27
   */
  accessor returnGeometryEnabled: boolean;
  /**
   * Indicates whether geometries fetched for the corresponding features contain M values, if supported.
   * The [returnGeometryEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#returnGeometryEnabled) property must also be true.
   *
   * > [!WARNING]
   * >
   * > Setting this to `true` can potentially impact the widget's performance.
   *
   * @default false
   * @since 4.30
   */
  accessor returnMEnabled: boolean;
  /**
   * Indicates whether the fetched features' geometries contain Z values. This is only applicable if Z-values are supported.
   * The [returnGeometryEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#returnGeometryEnabled) property must also be true.
   *
   * > [!WARNING]
   * >
   * > Setting this to `true` can potentially impact the widget's performance.
   *
   * @default false
   * @since 4.30
   */
  accessor returnZEnabled: boolean;
  /**
   * This property accepts and returns a collection of feature object IDs. It is used to access and control which rows display a darker background, i.e., highlighted. Take note that highlighted rows are not considered selected as this property is independent of the table's selection state. Use the [highlightIds](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#highlightIds) property to choose rows. Setting `rowHighlightIds` applies an alternative highlight style to an entire row or rows.
   *
   * >
   * > Use this property to highlight rows based on an event or action from another component (e.g. the view). For example, purposefully highlighting rows when hovering over a feature on the map or applying a "filter" via another widget to call out specific rows in the table. Generally, this property is not modified based on table events but rather an event or action from a different component.
   *
   * @since 4.30
   * @example
   * // This snippet adds an event listener which highlights (not selects) a row in the table
   * // when a mouse pointer hovers over its corresponding row.
   * featureTable.on("cell-pointerover", (event) => {
   * // Add the event's feature to the rowHighlightIds collection if it's not already included
   *   if (!featureTable.rowHighlightIds.includes(event.objectId)) {
   *     featureTable.rowHighlightIds.push(event.objectId);
   *   }
   * });
   * // This snippet adds an event listener which removes the highlight from a row in the table
   * // when a mouse pointer moves out of its corresponding row.
   * featureTable.on("cell-pointerout", (event) => {
   *   // Remove the event's feature from the rowHighlightIds collection if it's included
   *   const index = featureTable.rowHighlightIds.indexOf(event.objectId);
   *   if (index > -1) {
   *     featureTable.rowHighlightIds.splice(index, 1);
   *   }
   * });
   */
  get rowHighlightIds(): Collection<ObjectId>;
  set rowHighlightIds(value: ReadonlyArrayOrCollection<ObjectId>);
  /**
   * Use this property to supply a custom selection manager that overrides the default selection manager.
   * This is useful when applications want to share selection sets between components, without relying on the view's selection manager. This property is ignored if [syncViewSelection](#syncViewSelection) is `true`.
   *
   * @since 5.0
   * @beta
   */
  accessor selectionManager: SelectionManager | null | undefined;
  /**
   * Total number of records currently displayed in the table. This property takes into account all active filters and
   * the current value of [maxSize](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#maxSize) if it is configured.
   *
   * @default 0
   * @since 4.30
   */
  get size(): number;
  /**
   * The state of the widget.
   *
   * Value | Description
   * ------|------------
   * disabled | Dependencies are missing and therefore the widget is disabled.
   * error | Widget failed to load (_added at version 4.30_).
   * loaded | Widget is ready to use.
   * loading | Widget is busy loading its resources.
   * ready | Dependencies are met and has valid property values but hasn't started the load process.
   *
   * @default "disabled"
   */
  get state(): FeatureTableState;
  /**
   * Indicates whether the table and associated layer support adding attachments with the current configuration.
   *
   * @since 4.33
   */
  get supportsAddAttachments(): boolean;
  /**
   * Indicates whether the table and associated layer support viewing attachments with the current configuration.
   *
   * @since 4.33
   */
  get supportsAttachments(): boolean;
  /**
   * Indicates whether the table and associated layer support deleting attachments with the current configuration.
   *
   * @since 4.33
   */
  get supportsDeleteAttachments(): boolean;
  /**
   * Defines whether or not the feature supports resizing
   * attachments. This depends on whether the
   * feature layer's [capabilities.attachment.supportsResize](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#capabilities) is set to `true`.
   *
   * @since 4.33
   */
  get supportsResizeAttachments(): boolean;
  /**
   * Indicates whether the table and associated layer support updating attachments with the current configuration.
   *
   * @since 4.33
   */
  get supportsUpdateAttachments(): boolean;
  /**
   * Indicates whether the table should synchronize the current [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate) being used based on
   * changes made to the table's UI.
   *
   * @default true
   * @since 4.33
   * @see [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate)
   */
  accessor syncTemplateOnChangesEnabled: boolean;
  /**
   * Indicates whether the FeatureTable should sync with the view's [selection manager](https://developers.arcgis.com/javascript/latest/references/core/views/SelectionManager/).
   * Enabling this does not automatically inherit the [selection sources](https://developers.arcgis.com/javascript/latest/references/core/views/SelectionManager/#sources) from the [SelectionManager](https://developers.arcgis.com/javascript/latest/references/core/views/SelectionManager/), (e.g. which layers or graphics collections the view listens to for selections). If you need specific selection sources, configure them separately via [SelectionManager.sources](https://developers.arcgis.com/javascript/latest/references/core/views/SelectionManager/#sources).
   *
   * Setting this property takes precedence over the [selectionManager](#selectionManager) property.
   *
   * @default false
   * @since 5.0
   * @beta
   */
  accessor syncViewSelection: boolean;
  /**
   * Controls whether the table should sync with the view's selection manager.
   * This does not include syncing selection sources, which must be configured independently.
   *
   * @internal
   */
  accessor syncWithViewSelectionManager: boolean;
  /**
   * A reference to top-level controller table. This property is applicable if this table is a related table, nested within and controlled by another table widget.
   *
   * @since 4.31
   */
  get tableController(): FeatureTable | null | undefined;
  /**
   * A reference to the instance of a table widget that is directly associated with this table. This only applies if this table is nested within another table widget. The parent table is responsible for creating this child table instance. The child table reacts to state changes on the parent table, including row selection.
   *
   * The parent table that this property references may also be nested, meaning it would refer to a different parent table. The top-level table can always be referenced via the [tableController](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableController) property. If only one nested table exists, then the values of [tableController](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#tableController) and `tableParent` are the same.
   *
   * @since 4.31
   */
  get tableParent(): FeatureTable | null | undefined;
  /**
   * The associated [template](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/) used for the feature table.
   *
   * The [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/) is where you configure how the feature table should display and set any associated properties for the table and its columns.
   *
   * > [!WARNING]
   * >
   * > The `TableTemplate` provides more fine-grained control over how the table is rendered within the application by offering more advanced configurations such as custom cell rendering, column formatting, and more. `TableTemplate` is useful for application-level development that remains within an application. This property differs from the [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate) property as that property should be used to access the table's settings across different applications. By using [attributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#attributeTableTemplate), the settings can be saved within a webmap or layer. Please refer to the [AttributeTableTemplate](https://developers.arcgis.com/javascript/latest/references/core/tables/AttributeTableTemplate/) and [TableTemplate](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/TableTemplate/)  documentation for more information.
   *
   * > [!WARNING]
   * >
   * > Take note that it is required to set the `type` property when creating column templates.
   *
   * @since
   * 4.24
   *
   * > [!WARNING]
   * >
   * > Take note that it is required to set the `type` property when creating column templates.
   * @example
   * const tableTemplate = new TableTemplate({
   *   columnTemplates: [ // takes an array of FieldColumnTemplate and GroupColumnTemplate
   *   { // autocasts to FieldColumnTemplate
   *     type: "field", // This must be set when creating field column templates
   *     fieldName: "ObjectId",
   *     direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *     initialSortPriority: 1 // This field's sort order takes the second-highest priority.
   *   },
   *   {
   *     type: "field",
   *     fieldName: "NAME",
   *     label: "Name",
   *     direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *     initialSortPriority: 0 // This field's sort order takes the highest priority
   *    },
   *    {
   *      type: "field",
   *      fieldName: "STATUS",
   *      label: "Status",
   *      direction: "asc", // In order to use initialSortPriority, make sure direction is set
   *      initialSortPriority: 2 // This field's sort order is prioritized after Name and ObjectId, respectively.
   *    }]
   * });
   */
  get tableTemplate(): TableTemplate | null | undefined;
  set tableTemplate(value: TableTemplateProperties | null | undefined);
  /**
   * The [TimeExtent](https://developers.arcgis.com/javascript/latest/references/core/time/TimeExtent/) in which to filter and display data within the FeatureTable widget. Setting this property directly on the widget or its [viewModel](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FeatureTableViewModel/#timeExtent) takes precedence over the layer's [FeatureLayer.timeExtent](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#timeExtent). If this property is set directly on the widget, the table will not refresh when the layer's [TimeExtent](https://developers.arcgis.com/javascript/latest/references/core/time/TimeExtent/) changes.
   *
   * @since 4.30
   * @see [FeatureLayer.timeExtent](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#timeExtent)
   * @example
   * // Filters the table to display only features that fit within the time extent
   * reactiveUtils.watch(
   *   () => timeSlider.timeExtent,
   *   (extent) => {
   *     featureTable.timeExtent = extent;
   *   }
   * );
   */
  get timeExtent(): TimeExtent | null | undefined;
  set timeExtent(value: TimeExtentProperties | null | undefined);
  /**
   * Dates and times displayed in the widget will be in terms of this time zone. If not supplied, the view's time zone is used (if available). Depending on the field type, individual columns may have their own unique time zone behavior when the time zone itself is `unknown`.
   *
   * > [!WARNING]
   * >
   * > The following considerations apply when working with date, time, and big integer field types:
   * >
   * > By default, the [FeatureTable](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/) displays timezones for `date` and `timestamp-offset` field [types](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Field/#type) reflecting the MapView's timezone. This timezone can be overridden by setting the table's [timeZone](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#timeZone) property.
   * > If the table's [view](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#view) isn't set, and the table's [Feature.timeZone](https://developers.arcgis.com/javascript/latest/references/core/widgets/Feature/#timeZone) isn't set, the table defaults to `system` time. The only time that this is not the case is when there is a [FeatureLayer.preferredTimeZone](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#preferredTimeZone) set on the table's layer. If the latter is true, the preferred time zone is used as opposed to `system`.
   *
   * @since 4.28
   */
  accessor timeZone: TimeZone | null | undefined;
  /**
   * Replacement title for the table widget. This can be a basic string or custom function that returns a string. This is useful in situations where it may be necessary to dynamically update titles based on the current state of the table.
   *
   * @since 4.30
   * @example
   * // Create the FeatureTable widget
   * const featureTable = new FeatureTable({
   *   // Set the title to a custom function that returns a string
   *   title : () => {
   *     if (!featureTable) {
   *       return;
   *     }
   *     const state = featureTable.state;
   *     switch (state) {
   *       case "loading":
   *         return "Loading layer data...";
   *       case "loaded":
   *         const title = featureTable.layer?.title;
   *         const rowCount = featureTable.size;
   *         const selectedCount = featureTable.highlightIds?.length ?? 0;
   *         return `${title} (${selectedCount} rows selected out of ${rowCount} total)`;
   *       case "error":
   *         return "Error loading layer.";
   *       default:
   *         return;
   *     }
   *   }
   * })
   */
  accessor title: FeatureTableTitleFunction | string | null | undefined;
  /** A reference to the [MapView](https://developers.arcgis.com/javascript/latest/references/core/views/MapView/), [LinkChartView](https://developers.arcgis.com/javascript/latest/references/core/views/LinkChartView/), or [SceneView](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/). This property must be set if wanting to highlight a corresponding feature within the map when a row is selected. */
  accessor view: MapViewOrSceneView | LinkChartView | null | undefined;
  /** The view model for this widget. This is a class that contains all the logic, (ie. properties and methods), that controls the widget's behavior. See the [FeatureTableViewModel](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/FeatureTableViewModel/) class to access all properties and methods on the widget. */
  get viewModel(): FeatureTableViewModel;
  set viewModel(value: FeatureTableViewModelProperties);
  /**
   * A flattened array of all top-level visible columns. Take note that this does not include nested columns.
   *
   * @since 4.33
   */
  get visibleColumns(): FeatureTableSupportedColumn[];
  /**
   * The visible elements that are displayed within the widget.
   * This property provides the ability to turn individual elements of the widget's display on/off.
   */
  get visibleElements(): VisibleElements;
  set visibleElements(value: VisibleElementsProperties);
  /**
   * Creates a pending edit with the specified parameters.
   * A pending edit is not immediately saved to the layer.
   *
   * @param edits - Pending edits to be cached. Edits must include a valid objectId and field name.
   * @since 5.0
   * @example
   * // Add pending edits for two cells in a single row
   * table.addPendingEdits(
   *   [
   *     {
   *       objectId: 1,
   *       updates: [
   *         { fieldName: "Hotel_Name", value: "My Hotel" },
   *         { fieldName: "Address", value: "123 Hotel Street" },
   *       ]
   *     }
   *   ]
   * );
   */
  addPendingEdits(edits: FeatureStoreEdits[]): void;
  /**
   * Deletes all the selected rows from the table.
   *
   * > [!WARNING]
   * >
   * > There must be at least one selected row within the table for this to work. Also, make sure that [editingEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#editingEnabled) is set to `true` and the underlying service data supports deletion.
   *
   * @param showWarningPrompt - Indicates whether to display a prompt warning that the selected features will be deleted. Default behavior is to show a warning dialog before proceeding.
   * @returns Resolves when the selection is deleted.
   * @since 4.25
   */
  deleteSelection(showWarningPrompt?: boolean): Promise<void>;
  /**
   * Cancels any pending edits. This might be edits for a specific field or column, or
   * all pending edits for a specific feature. If no parameters are supplied, all pending edits
   * for all features are discarded. Additionally, if showWarningPrompt is 'true', the value returned
   * from this method will reflect whether or not the user chose to discard the pending edits.
   *
   * @param edits - Information about which pending edits should be canceled. If nothing is provided, all pending edits are discarded.
   * @param showWarningPrompt - Indicates the table should display a warning prompt, which allows users to confirm if pending edits should be discarded.
   * @since 5.0
   * @example
   * // Discard pending edits for single cell in a given row
   * table.discardPendingEdits([{ objectId: 1, fieldName: "Address" }]);
   *
   * // Discard pending edits for all cells in a given row
   * table.discardPendingEdits([{ objectId: 1 }]);
   *
   * // Discard all pending edits
   * table.discardPendingEdits();
   *
   * // Displays a confirmation prompt before discarding all pending edits
   * // Returns 'true' if edits were discarded
   * const editsWereDiscarded = await table.discardPendingEdits(undefined, true);
   */
  discardPendingEdits(edits?: DiscardPendingEditsParameters[] | null | undefined, showWarningPrompt?: boolean): Promise<boolean>;
  /**
   * Exports features associated with currently selected rows to a CSV file and displays a download prompt.  It automatically includes geometry for features with a `point` geometry type if invoked from the table's menu. Geometry information is excluded on all other layer types.
   *
   * > [!WARNING]
   * >
   * > **Known limitations**
   * > This method must be used on selected rows within the table.
   * > Geometry information is only exported for for features with a `point` geometry type.
   * > Multi-part geometries are not supported.
   *
   * @param includeGeometry - Indicates whether the geometry should be included in the exported file. Only applicable to features with `point` geometries.
   * @returns Resolves when the export dialog is displayed.
   * @since 4.31
   */
  exportSelectionToCSV(includeGeometry?: boolean): Promise<void>;
  /**
   * Finds the specified column within the feature table.
   *
   * @param fieldName - The `fieldName` of the column to find. This should match the field name as defined at the feature service level.
   * @returns The returned column.
   * @since 4.16
   */
  findColumn(fieldName: string): GridSupportedColumns | null | undefined;
  /**
   * Returns information about the feature with the provided object ID.
   *
   * @param objectId - The `ObjectId` field of the specified feature.
   * @returns Information about the associated feature.
   * @since 5.0
   */
  getFeatureStoreItemByObjectId(objectId: ObjectId): FeatureStoreItem | null | undefined;
  /**
   * Instructs the table to scroll to or display a specific page of data.
   *
   * @param index - Index of the page the table should display.
   * @since 4.31
   * @see [pageIndex](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#pageIndex)
   */
  goToPage(index: number): void;
  /**
   * Hides the specified column from the feature table.
   *
   * @param fieldName - The field name of the column to hide. This should match the field name as defined by the data source.
   * @since 4.16
   */
  hideColumn(fieldName: string): void;
  /**
   * Instructs the table to scroll to or display the next page of data.
   *
   * @since 4.31
   */
  nextPage(): void;
  /**
   * Instructs the table to scroll to or display the previous page of data.
   *
   * @since 4.31
   */
  previousPage(): void;
  /**
   * Refreshes the table contents while maintaining the current scroll position.
   *
   * @returns Resolves when the table finishes refreshing.
   * @since 4.16
   */
  refresh(): Promise<void>;
  /**
   * Re-renders the cell's content. Generally, this is only required if using a custom function for [FieldColumnTemplate.formatFunction](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#formatFunction) or [ColumnTemplate.formatFunction](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/ColumnTemplate/#formatFunction). If the rendering function is dependent on external data or its state, and that data or state changes, calling this method will re-render the cell content. If the function is dependent on the feature's attributes, calling this method is not necessary as the cell content will automatically update when the feature's attributes change. The same applies if the function is dependent on the feature's geometry. Calling this method is not necessary as the cell content will automatically update when the feature's geometry changes.
   *
   * @since 4.30
   */
  refreshCellContent(): void;
  /**
   * Attempts to save any pending edits. If any edits are invalid, the save will be rejected until the edits are in a valid state.
   *
   * @returns Resolves when the edits are saved or rejected.
   * @since 5.0
   */
  savePendingEdits(): Promise<void>;
  /**
   * Resets the horizontal scroll position of the table to the default view.
   *
   * @since 4.31
   */
  scrollLeft(): void;
  /**
   * Scrolls the table to the bottom row. If [pagination is enabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled), the table scrolls to the bottom row of the current page.
   *
   * @since 4.31
   */
  scrollToBottom(): void;
  /**
   * Scrolls the table to a row based on a specified index.
   *
   * @param index - Index of the row in which the table should scroll.
   * @since 4.19
   */
  scrollToIndex(index: number): void;
  /**
   * Scrolls the table to a row based on a specified object ID. The row must already be loaded for this method to take effect.
   *
   * @param objectId - Object id of a row's associated feature in which the table should scroll.
   * @since 4.31
   */
  scrollToRow(objectId: ObjectId): void;
  /**
   * Scrolls the table to the top row. If [pagination is enabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#paginationEnabled), the table scrolls to the top row of the current page.
   *
   * @since 4.31
   */
  scrollToTop(): void;
  /**
   * Shows all of the columns in the table.
   *
   * @since 4.16
   */
  showAllColumns(): void;
  /**
   * Shows the specified column within the feature table.
   *
   * @param fieldName - The `fieldName` of the column to show. This should match the field name as defined at the data source.
   * @since 4.16
   */
  showColumn(fieldName: string): void;
  /**
   * Sorts the column in either ascending ("asc") or descending ("desc") order.
   *
   * @param fieldName - The specified field name to sort. This should match the field name as defined at the data source.
   * @param direction - The direction to sort.
   * @since 4.16
   * @see [multiSortEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#multiSortEnabled)
   * @see [FieldColumnTemplate.initialSortPriority](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/support/FieldColumnTemplate/#initialSortPriority)
   */
  sortColumn(fieldName: string, direction: Direction): void;
  /**
   * Toggles current visibility of the provided column.
   *
   * @param fieldName - Associated field name of the column to be hidden or made visible. This should match the field name as defined at the data source.
   * @since 4.30
   */
  toggleColumnVisibility(fieldName: string): void;
  /**
   * Zooms the view to the extent of the current row selection. This can also be triggered as a menu item within the table. This item will display if at least one row is selected and the [view](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#view) is set on the `FeatureTable`.
   *
   * @since 4.23
   * @see [FeatureTable.visibleElements.menuItems.zoomToSelection](https://developers.arcgis.com/javascript/latest/references/core/widgets/FeatureTable/#visibleElements)
   */
  zoomToSelection(): Promise<void>;
}