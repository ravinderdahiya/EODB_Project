/**
 * Basemap utils for Components.
 *
 * @internal
 * @internal
 */
import type Basemap from "../../Basemap.js";

/**
 * Get the thumbnail url of an arcgis basemap.
 *
 * @param basemap
 * @internal
 * @internal
 */
export function getBasemapThumbnailUrl(basemap: Basemap | null | undefined): string | null | undefined;