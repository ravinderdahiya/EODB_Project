import type ManagedFBOAttachment from "./ManagedFBOAttachment.js";

/**
 * Describes a single attachment of a managed framebuffer. A managed color
 * attachment is also memory managed by the [SceneView](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/).
 *
 * @since 5.0
 */
export default abstract class ManagedColorAttachment extends ManagedFBOAttachment {}