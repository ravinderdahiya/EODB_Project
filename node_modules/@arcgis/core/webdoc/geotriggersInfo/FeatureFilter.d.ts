import type { ClonableMixin } from "../../core/Clonable.js";
import type { JSONSupport } from "../../core/JSONSupport.js";
import type { GeometryUnion } from "../../geometry/types.js";
import type { MeshProperties } from "../../geometry/Mesh.js";
import type { PolylineProperties } from "../../geometry/Polyline.js";
import type { PolygonProperties } from "../../geometry/Polygon.js";
import type { PointProperties } from "../../geometry/Point.js";
import type { MultipointProperties } from "../../geometry/Multipoint.js";
import type { ExtentProperties } from "../../geometry/Extent.js";

export interface FeatureFilterProperties extends Partial<Pick<FeatureFilter, "where">> {
  /** An optional geometry used to filter the features from a feature table. Any features that intersect the area of interest will be used. It can be any of the supported [geometry types](https://developers.arcgis.com/javascript/latest/references/core/views/layers/FeatureLayerView/#queryFeatures) */
  geometry?: ((ExtentProperties & { type: "extent" }) | (MultipointProperties & { type: "multipoint" }) | (PointProperties & { type: "point" }) | (PolygonProperties & { type: "polygon" }) | (PolylineProperties & { type: "polyline" }) | (MeshProperties & { type: "mesh" })) | null;
}

/**
 * Description of spatial and attribute filters that will be applied to Feature data. For example, used in Fence Parameters for Geotriggers.
 *
 * @since 4.24
 * @example
 * let filter = new FeatureFilter({
 *   where: "facility = 6",
 *   geometry: {
 *     x: 13871520.850500003,
 *     y: 3910293.086000003,
 *     spatialReference: {
 *       wkid: 102100,
 *       latestWkid: 3857
 *     }
 *   }
 * })
 */
export default class FeatureFilter extends FeatureFilterSuperclass {
  constructor(properties?: FeatureFilterProperties);
  /** An optional geometry used to filter the features from a feature table. Any features that intersect the area of interest will be used. It can be any of the supported [geometry types](https://developers.arcgis.com/javascript/latest/references/core/views/layers/FeatureLayerView/#queryFeatures) */
  get geometry(): GeometryUnion | null | undefined;
  set geometry(value: ((ExtentProperties & { type: "extent" }) | (MultipointProperties & { type: "multipoint" }) | (PointProperties & { type: "point" }) | (PolygonProperties & { type: "polygon" }) | (PolylineProperties & { type: "polyline" }) | (MeshProperties & { type: "mesh" })) | null | undefined);
  /** An optional SQL-based definition expression string that narrows the data to be used. Any features that satisfy the query will be used. */
  accessor where: string | null | undefined;
}
declare const FeatureFilterSuperclass: typeof JSONSupport & typeof ClonableMixin