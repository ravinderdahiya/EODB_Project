import type { ClonableMixin } from "../../core/Clonable.js";
import type { JSONSupport } from "../../core/JSONSupport.js";

/** @since 5.0 */
export interface VoxelRangeFilterProperties extends Partial<Pick<VoxelRangeFilter, "enabled" | "range">> {}

/**
 * VoxelRangeFilter defines the data range which will be rendered.
 *
 * @since 5.0
 */
export default class VoxelRangeFilter extends VoxelRangeFilterSuperclass {
  constructor(properties?: VoxelRangeFilterProperties);
  /**
   * Whether or not the filter is enabled.
   *
   * @default false
   * @since 5.0
   */
  accessor enabled: boolean;
  /**
   * The data range to render, specified as [minimum, maximum].
   *
   * @since 5.0
   */
  accessor range: [
      number,
      number
  ] | null;
}
declare const VoxelRangeFilterSuperclass: typeof JSONSupport & typeof ClonableMixin